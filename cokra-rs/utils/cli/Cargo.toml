# Cokra Utils - CLI
# CLI utilities

[package]
name = "cokra-cli"
version.workspace = true
edition.workspace = true

[dependencies]
anyhow = { workspace = true }
tracing = { workspace = true }
